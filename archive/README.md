# Archive

This directory holds frozen historical material for the legacy He-LMAS track inside the RegimeLift repository.

## Contents

- `legacy-he-lmas/`: the original heterogeneous KV-bridge project entry point and notes.
- `legacy-he-lmas/code/`: frozen legacy root code moved from the repository top level (`configs/`, `scripts/`, `src/`, `tests/`).

The archive is read-only in practice: new work should happen under the active RegimeLift code in `helmas3n/` and the `paper/` draft.
