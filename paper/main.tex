\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{longtable}
\usepackage{array}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{amsmath, amssymb, amsthm}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{enumitem}
\usepackage{tikz}
\usepackage{pgfplots}
\usepackage{verbatim}
\usepackage{float}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, calc}
\pgfplotsset{compat=1.18}

\newcommand{\method}[1]{\texttt{#1}}
\newcommand{\code}[1]{\texttt{#1}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\low}{\mathrm{low}}
\newcommand{\full}{\mathrm{full}}
\newcommand{\KL}{\mathrm{KL}}

\title{RegimeLift: Targeted Residual Uplift Across Nested Gemma 3n Operating Regimes}
\author{Marcel\\Independent Researcher}
\date{\today}

\begin{document}
\maketitle

\begin{abstract}
This paper studies state uplift across nested operating regimes in Gemma 3n. Gemma 3n is distributed as separate E2B and E4B artifacts, but its MatFormer-style design makes the reduced E2B-like regime part of the same parent family rather than an unrelated checkpoint. That architectural fact motivates a narrower and more testable question than heterogeneous model bridging: can internal state from the reduced operating regime be corrected just enough for decoding to resume under the fuller regime with improved behavioral fidelity? We investigate this question using paired low/full runs on identical prompt prefixes, residual-state translators with null controls, live handoff metrics at horizons 1/4/8/16, and a sequence of kill-tests that isolate where intervention actually matters. The main empirical result is that broad residual training is insufficient, while a targeted residual uplift at \method{layer34,last1} produces strong continuation recovery on a heldout80 split. A completed fixed-site objective ablation further improves this result, with a short-horizon objective achieving $h8=0.1328$ and $h16=0.0742$. A subsequent intermediate$\rightarrow$full transfer experiment shows that this intervention site remains effective beyond the original regime pair, indicating that the dominant bottleneck is intervention placement and regime-specific control rather than generic state-fitting capacity.
\end{abstract}

\section{Introduction}
The project in this repository began with a broader bridge hypothesis: can one transplant reasoning state across distinct language models? That hypothesis was too unconstrained. RegimeLift narrows the problem to a setting where structural relatedness is real and measurable: regime handoff inside Gemma 3n's shared E2B/E4B family.

Gemma 3n is documented as a nested / selective-activation architecture in which reduced and fuller operating modes coexist inside one parent family \cite{gemma3n2025,mrl2022}. The E2B and E4B artifacts are published separately, but the scientific object of interest here is not transfer between unrelated checkpoints. It is uplift between two operating regimes that were trained as part of the same nested family. That difference matters. If the reduced regime is an embedded sibling of the fuller regime, then a useful handoff might require only a small corrective transport map rather than a universal latent bridge.

This paper asks a deliberately limited question:
\begin{quote}
Can a low-activation Gemma 3n prefix state be uplifted into the corresponding fuller-regime state so that decoding resumes closer to native full-regime behavior?
\end{quote}

The answer is now evidence-based rather than speculative. Across completed separation tests, null controls, layer sweeps, suffix-span sweeps, and targeted-site studies, the work supports four concrete claims:
\begin{enumerate}[leftmargin=1.4em]
    \item low/full regimes are genuinely separated;
    \item identity is an exact control for the live handoff path;
    \item local state alignment alone is not enough;
    \item a specific learned intervention site, \method{layer34,last1}, yields the first robust continuation recovery result.
\end{enumerate}

\paragraph{Contributions.}
The current draft makes four contributions.
\begin{itemize}[leftmargin=1.4em]
    \item It formulates same-parent regime uplift in Gemma 3n as a falsifiable handoff problem rather than a generic manifold-alignment claim.
    \item It introduces a paired-state extraction and evaluation pipeline that measures both local alignment and resumed decoding behavior.
    \item It shows empirically that intervention site dominates broad translator capacity: broad MLP training improves local metrics, but targeted training at the correct site recovers behavior.
    \item It reports a completed heldout80 validation plus fixed-site objective ablation, establishing \method{layer34,last1} with a short-horizon objective as the best current learned handoff setting, and a first intermediate$\rightarrow$full transfer result showing that this late handoff site is not unique to the original regime pair.
\end{itemize}

\section{Background and framing}
\subsection{From archived He-LMAS to RegimeLift}
The archived He-LMAS project attempted heterogeneous cross-model transfer. That historical work remains useful as context, but it is not the active research track \cite{helmasLegacy2026}. RegimeLift changes the problem class:
\begin{itemize}[leftmargin=1.4em]
    \item Archived He-LMAS: transfer between different models.
    \item RegimeLift: uplift between nested operating regimes of the same parent family.
\end{itemize}

That reframing is not cosmetic. It replaces an ambitious but weakly constrained hypothesis with a narrower one whose failure modes can be isolated experimentally.

\subsection{Why Gemma 3n is the right testbed}
Gemma 3n is explicitly documented as a nested family with selective activation and Mix-n-Match style operating regimes \cite{gemma3n2025}. The reduced E2B-like path and the fuller E4B path are therefore related in a stronger sense than two arbitrary checkpoints. They are separate published artifacts, but they still belong to one shared structural family. For this paper, the important implication is:
\begin{quote}
the reduced path is different enough to create a real handoff problem, yet related enough that a small corrective map is plausible.
\end{quote}

That is the central scientific bet behind RegimeLift.

\section{Related work and positioning}
RegimeLift sits between two literatures. The first is standard transformer language modeling \cite{vaswani2017attention}, which gives the baseline architecture and the residual-stream object we intervene on. The second is nested-representation or Matryoshka-style modeling \cite{mrl2022}, which makes same-family operating-mode comparisons scientifically plausible in the first place.

What RegimeLift does \emph{not} claim is equally important. This is not a universal latent-alignment paper, not a cross-family checkpoint-bridging claim, and not evidence that one model's reasoning can always be transplanted into another. The archived He-LMAS line explored that broader space \cite{helmasLegacy2026}. The current work is intentionally narrower: one parent Gemma family, paired prefix states, and handoff quality measured by resumed decoding rather than geometric aesthetics.

That narrower framing is a strength. It allows the experiments to distinguish three different possibilities:
\begin{enumerate}[leftmargin=1.4em]
    \item the regimes are too similar to justify the project;
    \item the regimes are separated, but only local fit is learnable;
    \item the regimes are separated and a learned intervention can actually recover behavior.
\end{enumerate}
The completed results are most consistent with the third case, but only at a specific intervention site.

\section{Problem formulation}
Let $r \in \{\low,\full\}$ denote the reduced and fuller regimes. For prompt prefix $x_{1:T}$, layer $\ell$, and token position $t$, define the residual-stream state
\[
z^{(r)}_{\ell,t} \in \R^d.
\]
The primary objective is to learn a transport map that predicts the fuller-regime state from the reduced-regime state at a chosen intervention site:
\[
\hat{z}^{(\full)}_{\ell,t} = f_\theta\!\left(z^{(\low)}_{\ell,t}, c\right)
= z^{(\low)}_{\ell,t} + \Delta_\theta\!\left(z^{(\low)}_{\ell,t}, c\right),
\]
where $c$ is optional conditioning metadata such as layer index, token position, and regime descriptor.

The project began with broad layer-conditioned translators, but the later evidence showed that targeting a specific site matters more than fitting a globally smooth map.

\subsection{Training objective}
For residual uplift, the objective family is the weighted combination
\[
\mathcal{L}
= \lambda_{\text{state}} \mathcal{L}_{\text{state}}
+ \lambda_{\text{logit}} \mathcal{L}_{\text{logit}}
+ \lambda_{\text{ce}} \mathcal{L}_{\text{ce}},
\]
with
\[
\mathcal{L}_{\text{state}}
= \left\lVert \hat{z}^{(\full)}_{\ell,t} - z^{(\full)}_{\ell,t} \right\rVert_2^2
+ \lambda_{\cos}\left(1 - \cos\!\left(\hat{z}^{(\full)}_{\ell,t}, z^{(\full)}_{\ell,t}\right)\right),
\]
and
\[
\mathcal{L}_{\text{logit}}
= \KL\!\left(
p^{(\full)}_{t+1} \,\middle\|\, p^{(\text{uplift})}_{t+1}
\right).
\]
The short-horizon cross-entropy term is
\[
\mathcal{L}_{\text{ce}}
= -\log p^{(\text{uplift})}_{t+1}\!\left(y^{(\full)}_{t+1}\right),
\]
where the target token $y^{(\full)}_{t+1}$ is taken from the fuller-regime next-token decision under the same prefix.

In the completed fixed-site ablation at \method{layer34,last1}, the best current setting is
\[
\lambda_{\text{state}}=1.0,\quad
\lambda_{\cos}=0.2,\quad
\lambda_{\text{logit}}=0.3,\quad
\lambda_{\text{ce}}=1.0,
\]
corresponding to the \texttt{short-horizon} objective reported later in Table~\ref{tab:objective-ablation}; this configuration corresponds to the best-performing objective in the fixed-site ablation and is therefore used as the default setting for all reported fixed-site results.

The deployment criterion is behavioral rather than geometric: improved continuation after handoff. Let $y^{(\full)}_{t+i}$ denote greedy continuation under the fuller regime and $\hat{y}_{t+i}$ the continuation after patching the reduced regime and resuming under the fuller regime. For horizon $H \in \{1,4,8,16\}$, define
\[
\mathrm{Match}@H
= \frac{1}{NH}\sum_{n=1}^N \sum_{i=1}^{H}
\mathbf{1}\!\left[\hat{y}^{(n)}_{t+i} = y^{(\full,n)}_{t+i}\right].
\]
The project only counts as successful when these continuation metrics improve over the proper no-patch baseline.

\section{Experimental design}
\subsection{Paired-state extraction}
For each prompt prefix, the same parent Gemma 3n family is run under two operating modes: a reduced E2B-like regime and a fuller E4B regime. The extractor records matched layer/token metadata and paired tensors at aligned positions. Each sample contains:
\begin{itemize}[leftmargin=1.4em]
    \item prompt identifier;
    \item token position;
    \item layer index;
    \item residual-stream state in both regimes;
    \item optional KV cache slices;
    \item optional sparse logits snapshots.
\end{itemize}

The current paper focuses on residual uplift first. KV uplift remains a secondary object because direct cache prediction is more brittle and was not yet shown to improve the central behavioral metrics.

\subsection{Regime construction}
The current paper does not treat the two operating regimes as vague labels. They are instantiated by concrete activation and KV-sharing controls exposed through the Gemma runner. Table~\ref{tab:regimecontrols} summarizes the low/full settings used in the completed sanity and pilot runs.

\begin{table}[H]
    \centering
    \small
    \input{generated/regime_controls_table.tex}
    \caption{Low/full regime controls used for the current RegimeLift study. These settings define the reduced E2B-like path and the fuller E4B path used throughout the paired-state pipeline.}
    \label{tab:regimecontrols}
\end{table}

Operationally, these controls do the following in our runner. \texttt{activation\_sparsity} sets the fraction of FFN channels disabled per layer, so higher values produce a narrower active path. \texttt{KV shared layers} enables cache-sharing behavior in later blocks of the reduced regime, which changes the attention-state object that must be handed off. \texttt{scale correction} toggles a regime-specific normalization correction used by the text path in this implementation. \texttt{active index} selects the active stream when Gemma emits multiple stream slots; we keep it fixed at index 0 for all runs in this paper. In model terms, these controls approximate a continuum from a narrower E2B-like computation path to fuller E4B activation by jointly changing FFN capacity and late-layer cache behavior.

\subsection{Model families and controls}
The completed experiments cover:
\begin{itemize}[leftmargin=1.4em]
    \item \method{identity}: no correction;
    \item \method{mean-delta}, global linear, and per-layer linear baselines;
    \item \method{lowrank} and \method{mlp} residual translators;
    \item same-site exact-substitution reference patches (formerly called ``oracle'' rows in earlier notes).
\end{itemize}

The strictest control is \method{identity}. If identity differs from the proper no-patch path, the evaluator is invalid. That check now passes exactly.

\subsection{Live handoff implementation}
Live evaluation uses a fixed patch procedure. For each prompt prefix, we run a low-regime prefill and extract the residual state at the intervention site. The learned model predicts a replacement state for that site, and a forward hook patches the selected decoder layer output during prefill.

In the main result configuration, the patch is rank-1 in sequence dimension (``last1''): only the final prefix token at the chosen layer is overwritten. For \method{layer34,last1}, the patch tensor is applied to layer 34, token position $t=T$, after the layer output is produced and before final-logit projection.

After patched prefill, decoding resumes under the full regime using the resulting cache; continuation metrics compare this path against native full-regime continuation on the same prompt. Identity control uses the same code path but patches with the low state itself, which is why identity must match no-patch exactly.

\subsection{Prompt families and split protocol}
The current evidence is built on synthetic but structured prompt families rather than unconstrained open-ended traces. The prompt pools include arithmetic, symbolic, logical, sequential, and constrained-completion templates. This was a deliberate design choice: the first job of the project was to establish whether the handoff premise is even falsifiable under cleanly paired conditions.

The completed tables in this draft therefore combine:
\begin{itemize}[leftmargin=1.4em]
    \item a pilot split used for fast kill-tests and site sweeps;
    \item a smaller held-out split used in the frozen targeted-site study;
    \item a completed heldout80 split used for larger-sample targeted-site validation.
\end{itemize}
The important discipline is that architectural changes are not justified until they survive the held-out handoff metric, not just the pilot tables.

\subsection{Evaluation ladder}
The evaluation hierarchy is intentionally brutal:
\begin{enumerate}[leftmargin=1.4em]
    \item low/full separation sanity checks;
    \item extractor alignment checks;
    \item null controls and broad learned baselines;
    \item live layer sweeps;
    \item suffix-span sweeps;
    \item targeted-site training and held-out validation.
\end{enumerate}

This sequence matters because it eliminates false-success paths. The project explicitly rejects the claim ``the translator works'' if only local MSE improves while continuation remains unchanged.

\begin{figure}[t]
    \centering
    \resizebox{0.95\linewidth}{!}{%
    \begin{tikzpicture}[
        node distance=1.35cm and 1.6cm,
        box/.style={rounded corners, draw=black!70, fill=black!3, align=center, minimum width=3.2cm, minimum height=1cm},
        arrow/.style={-{Latex[length=3mm]}, very thick}
    ]
        \node[box] (src) {Reduced Gemma 3n\\E2B-like regime};
        \node[box, right=of src] (pair) {Paired extraction\\same prefix, same positions};
        \node[box, right=of pair] (train) {Residual uplift\\null controls + learned maps};
        \node[box, below=of train] (eval) {Live handoff evaluation\\Match@1/4/8/16};
        \node[box, left=of eval] (oracle) {Reference patches\\single-site and sparse multi-layer};
        \node[box, left=of oracle] (memo) {Decision memo\\paper draft\\frozen experiments};

        \draw[arrow] (src) -- (pair);
        \draw[arrow] (pair) -- (train);
        \draw[arrow] (train) -- (eval);
        \draw[arrow] (oracle) -- (eval);
        \draw[arrow] (memo) -- (oracle);
        \draw[arrow] (pair) |- (oracle);
    \end{tikzpicture}
    }
    \caption{Active RegimeLift pipeline. The work progresses from paired extraction to residual uplift and then to live continuation handoff.}
    \label{fig:pipeline}
\end{figure}

\section{Experiment sequence}
The evidence in this paper is the output of a fixed sequence of kill-tests rather than one monolithic run. Table~\ref{tab:timeline} records that sequence.

\begin{table}[H]
    \centering
    \small
    \input{generated/experiment_timeline.tex}
    \caption{Chronology of the completed experiment program and the gate answered by each phase.}
    \label{tab:timeline}
\end{table}

The key transition in that sequence was from broad training to targeted intervention. Broad residual training improved local state metrics but did not recover behavior strongly enough. The subsequent layer and span sweeps isolated the intervention surface before targeted training was attempted.

\section{Results}
\subsection{Regime separation sanity}
Before any uplift training, the project must establish that the reduced and fuller regimes are meaningfully different. Table~\ref{tab:separation} and Figure~\ref{fig:separation} summarize that first gate using the completed sanity report.

\begin{table}[H]
    \centering
    \small
    \input{generated/regime_separation_table.tex}
    \caption{Global low/full separation metrics from the completed sanity report. These values establish that the handoff problem is real before any translator is trained.}
    \label{tab:separation}
\end{table}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\linewidth]{figures/regime_separation_profile.pdf}
    \caption{Per-layer low/full separation profile. The mismatch is not uniform across the stack, which is one reason later intervention-site sweeps are necessary.}
    \label{fig:separation}
\end{figure}

The important facts here are straightforward. The mean residual cosine is near 0.50 rather than near 1.0, the mean next-token KL is large, and top-1 agreement is low. This is enough to rule out the trivial failure mode in which low and full regimes are operationally identical under the current runner.

\subsection{Core targeted-site result}
The strongest completed result is the targeted site study summarized in Table~\ref{tab:core} and Figure~\ref{fig:targeted-curves}. The contrast is decisive:
\begin{itemize}[leftmargin=1.4em]
    \item \method{identity} matches \method{no patch} exactly;
    \item broad MLP training improves over the no-patch baseline only weakly;
    \item targeted MLP at \method{layer16,last1} fails to generalize;
    \item targeted MLP at \method{layer34,last1} produces the first strong held-out continuation recovery result.
\end{itemize}

\begin{table}[H]
    \centering
    \small
    \caption{Heldout80 continuation match versus full-regime decoding for the completed targeted-site study. The split contains 80 prompts: arithmetic (21), logic (19), symbolic (17), sequence (12), and constrained generation (11).}
    \resizebox{\linewidth}{!}{\input{generated/core_results_table.tex}}
    \label{tab:core}
\end{table}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\linewidth]{figures/targeted_handoff_curves.pdf}
    \caption{Continuation match curves for the targeted-site study. The practical result is the separation between broad training and targeted training at \method{layer34,last1}.}
    \label{fig:targeted-curves}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\linewidth]{figures/targeted_delta_summary.pdf}
    \caption{Heldout80 absolute gains over no-patch at horizons 8 and 16. The dominant learned gain comes from the targeted \method{layer34,last1} model; broad training and the failed \method{layer16,last1} target do not recover comparable behavior.}
    \label{fig:targeted-delta}
\end{figure}

The most important row in Table~\ref{tab:core} is \method{Targeted MLP @ layer34}. On the completed heldout80 split it achieves Match@1 of 0.775, Match@4 of 0.203, Match@8 of 0.116, and Match@16 of 0.064. No other learned row comes close. Importantly, \method{layer16,last1} looked plausible in pilot sweeps and then failed under stronger validation, which is exactly why targeted-site validation was necessary. This is the first point in the project where a learned intervention does what the project is supposed to do: materially recover continuation behavior on held-out prompts.

\subsection{Fixed-site objective ablation}
The objective-ablation phase holds the intervention fixed at \method{layer34,last1} and changes only loss composition. Table~\ref{tab:objective-ablation} exposes the full matrix that was previously summarized in prose: \texttt{state-only}, \texttt{state+logit}, \texttt{heavy-logit}, and \texttt{short-horizon}. The ranking is consistent across horizons and deltas: the short-horizon objective is strongest, heavy-logit is second, and state-only is weakest.

\begin{table}[H]
    \centering
    \small
    \caption{Heldout80 objective-ablation matrix at fixed \method{layer34,last1}. Deltas are measured against the same no-patch baseline.}
    \resizebox{\linewidth}{!}{\input{generated/objective_ablation_table.tex}}
    \label{tab:objective-ablation}
\end{table}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\linewidth]{figures/objective_ablation_delta.pdf}
    \caption{Objective-ablation gains over no-patch at horizons 8 and 16. The short-horizon objective provides the strongest behavioral recovery under a fixed intervention site.}
    \label{fig:objective-ablation}
\end{figure}

\subsection{Intermediate-to-full transfer}
A completed intermediate-to-full targeted-site study shows that the same late handoff interface remains useful beyond the original minimum-to-full setting. At \method{layer34,last1}, the targeted MLP reduces heldout Match@8 error from 0.00625 to 0.00000 and Match@16 error from 0.003125 to 0.001563, while broad MLP and identity leave the baseline unchanged. Layer16 yields only weaker or inconsistent gains. This pattern supports a restrained transfer claim: the effective intervention site is not unique to one extreme regime pair, but transfers to at least one adjacent regime transition within the same family.

\subsection{Full-like versus low-like alignment}
Table~\ref{tab:full-vs-low} addresses whether learned uplift is merely perturbing continuation arbitrarily or moving behavior toward fuller-regime decoding. The targeted \method{layer34} row raises match-to-full-native strongly over no-patch and broad MLP while keeping match-to-low-native low, which is consistent with the intended directional effect. By contrast, broad MLP remains closer to low-native behavior than to full-native behavior.

\begin{table}[H]
    \centering
    \small
    \caption{Next-token behavioral alignment at \method{layer34,last1}: full-native versus low-native.}
    \resizebox{\linewidth}{!}{\input{generated/full_vs_low_table.tex}}
    \label{tab:full-vs-low}
\end{table}

\subsection{Layer sensitivity}
The corrected layer sweep showed that continuation recovery is controlled by a narrow intervention surface. Earlier pilot impressions suggested \method{layer16} might be the right target. The controlled sweep falsified that as a stable main site and instead showed that the strongest useful region lies substantially later in the stack.

\begin{table}[H]
    \centering
    \small
    \input{generated/layer_sweep_summary_table.tex}
    \caption{Summary of the corrected layer sweep. The learned and reference-patch peaks are not uniform across the stack.}
    \label{tab:layersweep}
\end{table}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\linewidth]{figures/layer_sweep_sensitivity.pdf}
    \caption{Layer sensitivity of live handoff. The main result is not just that some layers are better than others, but that broad training was washing out the signal at the truly useful site.}
    \label{fig:layersweep}
\end{figure}

\subsection{Suffix-span sensitivity}
Once the correct intervention site was isolated, the next question was whether the patch needed more recent context than the final prefix token. The completed suffix-span sweep says mostly no: widening from the last token to the last 4 or 8 tokens did not produce a comparable gain to moving the intervention to the right site.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\linewidth]{figures/suffix_span_sensitivity.pdf}
    \caption{Suffix-span sensitivity at the selected intervention sites. Patch width matters less than intervention site in the completed pilot.}
    \label{fig:suffix}
\end{figure}

\subsection{What the evidence now supports}
The current evidence justifies the following claims:
\begin{itemize}[leftmargin=1.4em]
    \item low/full Gemma 3n operating regimes are measurably distinct;
    \item the live handoff path is valid because identity equals proper no-patch exactly;
    \item local residual fit is insufficient as a success criterion;
    \item the first robust learned handoff site is \method{layer34,last1};
    \item fixed-site objective ablation at \method{layer34,last1} improves continuation further, with the short-horizon objective reaching $h8=0.1328$ and $h16=0.0742$ on heldout80;
    \item a first intermediate$\rightarrow$full transfer result shows that the same late handoff site remains behaviorally useful beyond the original minimum$\rightarrow$full pair;
    \item same-site exact substitution is a reference patch, not a hard behavioral ceiling;
    \item intervention placement is a more important bottleneck than broad translator capacity.
\end{itemize}

\section{Discussion}
\subsection{Why this is not just ``two different models''}
The right framing is neither extreme. The reduced and fuller regimes are not identical, and the published E2B and E4B artifacts are exposed separately. But for the purposes of this project they are still nested operating regimes of one parent family, trained and structured together rather than unrelated standalone models. That is why same-parent uplift is a coherent hypothesis and why the experiments can legitimately ask whether a small corrective transport suffices.
The evidence in this paper suggests that reduced and fuller regimes preserve overlapping task-relevant information while organizing it in different internal coordinates. In that view, successful handoff is best treated as regime-conditioned representational translation rather than simple capacity restoration.

\subsection{Why \method{layer34,last1} changes the project}
Before the targeted-site study, the project had evidence of regime separation and local learnability but not of meaningful behavioral recovery. The \method{layer34,last1} result changes that. It turns the work from ``interesting internal geometry'' into a practical handoff result with held-out support.

The negative result is equally important. \method{layer16,last1} looked plausible in earlier sweeps and failed under stronger validation. That failure made the final result more credible because it showed the project was not simply confirming whichever site looked best in an early pilot.

\subsection{Why the reference patch is not a ceiling}
At some horizons the learned row exceeds the same-site exact-substitution reference. That means raw state fidelity is not identical to optimal rollout behavior. The sensible interpretation is that the reference patch is a control surface, not a mathematical upper bound for continuation match. This paper therefore uses the term \emph{reference patch} consistently instead of \emph{oracle ceiling}.

\subsection{Inference relevance}
The intended systems use-case is small-first decoding with selective escalation: run the reduced regime when possible, then hand off to fuller activation when continuation quality requires it. This paper establishes the handoff feasibility component, not a full serving policy.

Cost accounting is therefore preliminary. In the completed heldout80 support run at \method{layer34,last1}, mean low-prefill cost was $283.3$ ms, mean full-restart cost was $3191.4$ ms, and mean low-plus-handoff pipeline cost was $3504.5$ ms, i.e., $+313.1$ ms slower than full restart in the current instrumentation-heavy setup. That means this paper should not be read as a throughput claim yet; it is a behavioral handoff claim. A production-relevant study still needs trigger policy design, cache-transfer overhead accounting, and clean end-to-end latency measurements under fixed hardware kernels.

\subsection{Limitations}
An earlier held-out validation accidentally used only 10 prompts. The evaluator was therefore hardened to fail fast on prompt-count mismatch and to write incremental artifacts, and the completed heldout80 rerun is now the canonical larger-sample result.

The other limitation is scope. This paper establishes one strong residual handoff site first. It does not yet claim that the same uplift map works uniformly across intermediate Mix-n-Match regimes, nor that KV uplift is already competitive.

\subsection{Threats to validity}
Three threats matter most at this stage.
\begin{itemize}[leftmargin=1.4em]
    \item \textbf{Prompt diversity.} The completed heldout80 result is real, but still limited in breadth relative to final publication standards. Additional held-out slices and intermediate-regime pairs remain necessary.
    \item \textbf{Single-site intervention.} RegimeLift has established a first useful site, not a full theory of all intervention surfaces. Sparse multi-layer reference patches remain stronger than many single-site learned interventions.
    \item \textbf{Objective mismatch.} The project already has evidence that excellent local residual fit does not guarantee good continuation recovery. This means future objective changes must be judged behaviorally, not cosmetically.
\end{itemize}

\subsection{Next experimental priority}
Fixed-site objective ablation has now been completed at \method{layer34,last1}. Among \texttt{state-only}, \texttt{state+logit}, \texttt{heavy-logit}, and \texttt{short-horizon} objectives, the short-horizon objective is the strongest on heldout80 continuation metrics. A first intermediate-regime transfer result (intermediate $\rightarrow$ full) confirms that the learned handoff site transfers beyond the original minimum $\rightarrow$ full setting, achieving strong recovery at short horizons and consistent gains at longer horizons.

The next step is to complete the regime triangle by evaluating minimum $\rightarrow$ intermediate under the same fixed site and objective, to determine whether this behavior reflects a broader regime-lift mechanism or remains pair-specific.

\section{Reproducibility and run record}
This repository keeps the paper tied to concrete experiment artifacts. Reproducibility rests on three pieces:
\begin{itemize}[leftmargin=1.4em]
    \item fixed prompt pools with explicit split files;
    \item explicit run manifests with resolved config paths and prompt counts;
    \item incremental row-level result dumps for interrupted-run recovery.
\end{itemize}
The full heldout80 manifest excerpt is provided in Appendix~\ref{app:runlog}.

\section{Conclusion}
RegimeLift now has a stable empirical spine. The project no longer rests on the vague claim that ``state translation might work.'' It now supports a sharper statement:
\begin{quote}
Within Gemma 3n's nested E2B/E4B family, low-to-full regime uplift is behaviorally recoverable at a specific learned handoff site, and broad residual fitting alone is not enough to find that site.
\end{quote}

That is the main result of the current paper. The next step is not to widen the architecture search blindly. It is to hold the best fixed-site objective at \method{layer34,last1} and complete the minimum$\rightarrow$intermediate test before expanding to KV-level or flow-based methods.

\bibliographystyle{plain}
\bibliography{references}

\appendix
\section{Heldout80 Run Manifest Excerpt}
\label{app:runlog}
\begin{table}[H]
    \centering
    \small
    \begin{minipage}{0.95\linewidth}
        \input{generated/log_excerpt.tex}
    \end{minipage}
    \caption{Manifest excerpt for the completed heldout80 validation run.}
\end{table}

\end{document}
