seed: 7

experiment:
  name: residual_mean_delta
  out_dir: artifacts/checkpoints/residual_mean_delta

data:
  root_dir: artifacts/paired_states
  target: residual
  batch_size: 64
  num_workers: 0

model:
  kind: mean_delta
  freeze_mean_delta: true

optimizer:
  lr: 0.0003
  weight_decay: 0.0

training:
  epochs: 1
  grad_clip: 1.0
  eval_every_steps: 200

loss:
  mse_weight: 1.0
  cosine_weight: 0.2
  logit_weight: 0.0

logit_head:
  enabled: false
  snapshot_path: artifacts/paired_states/logit_head.pt

evaluation:
  max_eval_batches: 50
  continuation_steps: 16
